import { Pool, Stats } from '../types/characterTypes';

export const BASE_ATTACK_DAMAGE = 10;
export const BASE_HEALTH: Pool = {
  current: 100,
  max: 100,
};
export const BASE_MANA: Pool = {
  current: 100,
  max: 100,
};
export const BASE_EXPERIENCE_LEVEL = 100;

export const BASE_STATS: Stats = {
  initiative: 0,
};
